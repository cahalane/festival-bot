{
  "name": "festival-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A festival schedule-planning bot for Claude Code — plans around clashes and walking times, mirrors to Clashfinder, watches for schedule changes.",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "festivals/*"
  ],
  "scripts": {
    "typecheck": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest",
    "cli": "bun packages/cli/src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "tsx": "^4.7.1",
    "typescript": "^5.4.5",
    "vitest": "^1.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
